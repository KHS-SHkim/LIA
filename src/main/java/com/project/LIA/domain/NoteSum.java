package com.project.LIA.domain;

public class NoteSum {
    private Long id;
    private UserDomain user;
    private UserDomain receiver;

    private Long receptionChk;

    public NoteSum(Long id, UserDomain user, UserDomain receiver, Long receptionChk) {
    }
}
